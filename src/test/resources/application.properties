spring.application.name=menufacil-test

# MongoDB embedded (não depende de localhost:27017)
spring.mongodb.embedded.version=7.0.12

# Desabilita criação de índices no startup durante testes de contexto
menufacil.mongo.create-indexes=false
